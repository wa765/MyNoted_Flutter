import 'package:flutter/material.dart';

class WidgetDrag extends StatelessWidget {
  const WidgetDrag({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
